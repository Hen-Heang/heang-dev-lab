<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heang.springmybatistest.mapper.UserMapper">

    <!-- ===================================
         Common Result Map
         =================================== -->
    <resultMap id="UserMap" type="com.heang.springmybatistest.model.Users">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>

    </resultMap>

    <!--
        [Reusable Column Fragment]

        <sql> defines a reusable SQL snippet in one place.
        Use it anywhere with: <include refid="UserColumns"/>

        Why use this?
        - DRY (Don't Repeat Yourself): write the column list only once
        - Maintenance: add/remove a column here and all queries update automatically
        - Consistency: every SELECT query fetches the same columns
    -->
    <sql id="UserColumns">
        id
        , username, email, password, name, phone, role, status, created_at, updated_at
    </sql>

    <!-- ===================================
         CRUD Queries
         =================================== -->

    <!-- INSERT -->
    <insert id="insertUser"
            parameterType="com.heang.springmybatistest.dto.UserRequest">
        INSERT INTO users (username,
                           email,
                           name,
                           phone,
                           password,
                           role,
                           status)
        VALUES (#{username},
                #{email},
                #{name},
                #{phone},
                #{password},
                COALESCE(#{role}, 'ROLE_USER'),
                COALESCE(#{status}, 'ACTIVE'))
    </insert>

    <!-- SELECT ALL -->
    <select id="selectUserList" resultMap="UserMap">
        SELECT
        <include refid="UserColumns"/>
        FROM users
        ORDER BY id DESC
    </select>

    <!-- SELECT BY ID -->
    <select id="selectUserById" parameterType="long" resultMap="UserMap">
        SELECT
        <include refid="UserColumns"/>
        FROM users
        WHERE id = #{id}
    </select>

    <!-- SEARCH + PAGINATION -->
    <select id="searchUsers" parameterType="com.heang.springmybatistest.dto.UserSearchRequest" resultMap="UserMap">
        SELECT
        <include refid="UserColumns"/><!-- reuse column list defined above -->
        FROM users

        <!--
            <where> : automatically adds WHERE keyword if at least one <if> condition is true.
                      if no conditions match, WHERE is skipped entirely — prevents invalid SQL.
        -->
        <where>
            <!-- only add this condition if username was sent (partial match: LIKE '%value%') -->
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>

            <!-- only add this condition if email was sent (partial match) -->
            <if test="email != null and email != ''">
                AND email LIKE CONCAT('%', #{email}, '%')
            </if>

            <!-- only add this condition if status was sent (exact match) -->
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>

            <!-- keyword searches both username and email at once (OR search) -->
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>

            <!--
                if ids list is not empty, generate IN clause
                e.g. ids=[1,2,3] → AND id IN (1, 2, 3)
                <foreach> loops over the list and joins items with comma separator
            -->
            <if test="ids != null and ids.size() > 0">
                AND id IN
                <foreach collection="ids" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </if>

            <!--
                if statuses list is not empty, generate IN clause
                e.g. statuses=['ACTIVE','INACTIVE'] → AND status IN ('ACTIVE', 'INACTIVE')
            -->
            <if test="statuses != null and statuses.size() > 0">
                AND status IN
                <foreach collection="statuses" item="st" open="(" close=")" separator=",">
                    #{st}
                </foreach>
            </if>

            <!-- &gt;= is XML-safe way to write >= (greater than or equal) -->
            <if test="startDate != null">
                AND created_at &gt;= #{startDate}
            </if>

            <!-- &lt;= is XML-safe way to write <= (less than or equal) -->
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>

        <!--
            <choose> works like Java switch/case — picks exactly one option.
            sortBy value decides which column to sort by.
            if nothing is sent, <otherwise> is used → default sort by id.
        -->
        ORDER BY
        <choose>
            <when test="sortBy == 'username'">username</when>
            <when test="sortBy == 'email'">email</when>
            <when test="sortBy == 'createdAt'">created_at</when>
            <when test="sortBy == 'status'">status</when>
            <otherwise>id</otherwise><!-- default sort column -->
        </choose>

        <!-- sort direction: ASC = oldest first, DESC = newest first (default) -->
        <choose>
            <when test="sortOrder == 'ASC'">ASC</when>
            <otherwise>DESC</otherwise><!-- default direction -->
        </choose>

        <!--
            Pagination: size = rows per page, offset = rows to skip
            e.g. page 3 with 10 per page → LIMIT 10 OFFSET 20
            only applied when both size and offset are provided
        -->
        <if test="size != null and offset != null">
            LIMIT #{size} OFFSET #{offset}
        </if>
    </select>

    <select id="countUsers" parameterType="com.heang.springmybatistest.dto.UserSearchRequest" resultType="int">
        SELECT COUNT(*)
        FROM users
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email LIKE CONCAT('%', #{email}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="ids != null and ids.size() > 0">
                AND id IN
                <foreach collection="ids" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </if>
            <if test="statuses != null and statuses.size() > 0">
                AND status IN
                <foreach collection="statuses" item="st" open="(" close=")" separator=",">
                    #{st}
                </foreach>
            </if>
            <if test="startDate != null">
                AND created_at &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND created_at &lt;= #{endDate}
            </if>
        </where>
    </select>

    <!-- UPDATE -->
    <update id="updateUser">
        UPDATE users
        SET username   = #{req.username},
            email      = #{req.email},
            status     = COALESCE(#{req.status}, status),
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- DELETE -->
    <delete id="deleteUser" parameterType="long">
        DELETE
        FROM users
        WHERE id = #{id}
    </delete>

    <!-- ===================================
         Spring Security Queries
         =================================== -->

    <!-- find by username (used for login) -->
    <select id="selectUserByUsername" parameterType="string" resultMap="UserMap">
        SELECT id,
               username,
               email,
               password,
               name,
               phone,
               role,
               status,
               created_at,
               updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <!-- find by email -->
    <select id="selectUserByEmail" parameterType="string" resultMap="UserMap">
        SELECT id,
               username,
               email,
               password,
               name,
               phone,
               role,
               status,
               created_at,
               updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- check if username already exists (duplicate check) -->
    <select id="countByUsername" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE username = #{username}
    </select>

    <!-- check if email already exists (duplicate check) -->
    <select id="countByEmail" parameterType="string" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <!-- register new user (with encoded password) -->
    <insert id="insertUserWithPassword" parameterType="com.heang.springmybatistest.model.Users"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password, name, phone, role, status)
        VALUES (#{username},
                #{email},
                #{password},
                #{name},
                #{phone},
                COALESCE(#{role}, 'ROLE_USER'),
                COALESCE(#{status}, 'ACTIVE'))
    </insert>

    <!-- update password -->
    <update id="updatePassword">
        UPDATE users
        SET password   = #{password},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- update last login time -->
    <update id="updateLastLogin" parameterType="long">
        UPDATE users
        SET updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
